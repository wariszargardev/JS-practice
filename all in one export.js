import {age, email} from "./In-line individually";

const uname = "Waris";
const uage = 25;
const uemail = "Waris@gmail.com";

const message = () =>{
    return "name " + name + " email " + email + " age " + age
}

export default message